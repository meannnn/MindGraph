# MindGraph 完整运行指南

## 🔧 解决 Docker 权限问题

如果您在 WSL/Linux 中遇到 Docker 权限问题，有两种解决方案：

### 方案 1：修复 Docker 权限（推荐）

```bash
# 将当前用户添加到 docker 组
sudo usermod -aG docker $USER

# 重新登录或刷新组权限
newgrp docker

# 验证 Docker 是否可用
docker ps
```

**注意**：执行 `newgrp docker` 后需要重新打开终端，或者注销重新登录。

### 方案 2：使用 sudo 运行 Docker（临时方案）

```bash
# 使用 sudo 运行 Docker 命令
sudo docker run -d -p 6379:6379 --name redis redis:7-alpine
sudo docker run -d -p 6333:6333 --name qdrant qdrant/qdrant
```

### 方案 3：不使用 Docker（推荐用于开发）

如果不想使用 Docker，可以直接在 WSL 中安装 Redis 和 Qdrant：

---

## 🚀 完整运行步骤（不使用 Docker）

### 第一步：在 WSL 中安装并启动 Redis

```bash
# 更新包列表
sudo apt update

# 安装 Redis
sudo apt install redis-server -y

# 启动 Redis 服务
sudo service redis-server start

# 设置开机自启（可选）
sudo systemctl enable redis-server

# 验证 Redis 是否运行
redis-cli ping
# 应该返回: PONG
```

### 第二步：在 WSL 中安装并启动 Qdrant

```bash
# 进入项目目录
cd /mnt/d/MindMate/MindGraph-main

# 使用项目提供的安装脚本
bash scripts/setup/install_dependencies.sh --qdrant-only

# 或者手动安装 Qdrant
# 下载 Qdrant 二进制文件
wget https://github.com/qdrant/qdrant/releases/download/v1.7.4/qdrant-x86_64-unknown-linux-gnu.tar.gz
tar -xzf qdrant-x86_64-unknown-linux-gnu.tar.gz
sudo mv qdrant /usr/local/bin/

# 启动 Qdrant（后台运行）
qdrant &

# 验证 Qdrant 是否运行
curl http://localhost:6333/collections
# 应该返回 JSON 数据
```

### 第三步：安装后端依赖

```bash
# 在项目根目录（Windows 路径）
cd /mnt/d/MindMate/MindGraph-main

# 运行安装脚本
python scripts/setup/setup.py

# 或者手动安装
pip install -r requirements.txt
playwright install chromium
```

### 第四步：安装前端依赖并构建

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 构建前端（生产模式）
npm run build

# 返回项目根目录
cd ..
```

**开发模式（可选）：**
```bash
cd frontend
npm run dev  # 前端开发服务器（热重载，端口 3000）
# 保持此终端运行，在另一个终端运行后端
```

### 第五步：检查环境变量配置

确保 `.env` 文件已正确配置：

```bash
# 检查关键配置
cat .env | grep -E "(QWEN_API_KEY|REDIS_URL|QDRANT_HOST|PORT)"
```

**必需配置项：**
- `QWEN_API_KEY` - 您的通义千问 API 密钥
- `REDIS_URL=redis://localhost:6379/0` - Redis 连接地址
- `QDRANT_HOST=localhost:6333` - Qdrant 连接地址（知识空间功能需要）
- `PORT=9527` - 服务器端口

### 第六步：运行服务器

```bash
# 在项目根目录运行
python main.py
```

服务器启动后，您会看到类似输出：
```
[REDIS] ✓ Redis is ready
[QDRANT] ✓ Qdrant is ready
[CELERY] ✓ Celery worker started
Starting server on http://0.0.0.0:9527
```

---

## 🌐 访问入口

启动成功后，访问以下地址：

| 服务 | URL | 说明 |
|------|-----|------|
| **交互式编辑器** | http://localhost:9527/editor | 完整功能的 Web 编辑器 |
| **API 文档** | http://localhost:9527/docs | Swagger UI（仅 DEBUG=True 时可用） |
| **管理面板** | http://localhost:9527/admin | API 密钥、用户、设置管理 |
| **健康检查** | http://localhost:9527/health | 服务器状态 |
| **知识空间** | http://localhost:9527/knowledge-space | 知识空间管理界面 |

---

## ⚠️ 重要提示

1. **Redis 必须在启动服务器前运行**，否则应用无法启动
2. **Qdrant 是知识空间功能必需的**，如果不需要该功能可以暂时不启动
3. **Celery 工作进程会自动启动**用于后台处理
4. **API 文档** (`/docs`) 仅在 `DEBUG=True` 时可用
5. 如果使用 WSL，确保在 WSL 环境中运行所有命令

---

## 🔧 故障排查

### Redis 连接失败

```bash
# 检查 Redis 是否运行
redis-cli ping

# 如果未运行，启动 Redis
sudo service redis-server start

# 检查 Redis 端口
netstat -tlnp | grep 6379
```

### Qdrant 连接失败

```bash
# 检查 Qdrant 是否运行
curl http://localhost:6333/collections

# 如果未运行，启动 Qdrant
qdrant &

# 检查 Qdrant 端口
netstat -tlnp | grep 6333
```

### 端口被占用

```bash
# 检查端口占用（Linux/WSL）
netstat -tlnp | grep 9527

# 或者使用 lsof
lsof -i :9527

# 修改 .env 中的 PORT 配置
# PORT=9528  # 使用其他端口
```

### Python 依赖安装失败

```bash
# 升级 pip
python -m pip install --upgrade pip

# 清除缓存重新安装
pip install --no-cache-dir -r requirements.txt
```

### 前端构建失败

```bash
cd frontend

# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install

# 重新构建
npm run build
```

### Docker 权限问题（如果使用 Docker）

```bash
# 方案 1：添加用户到 docker 组
sudo usermod -aG docker $USER
newgrp docker

# 方案 2：使用 sudo（不推荐）
sudo docker run ...

# 方案 3：不使用 Docker，直接安装服务
# 参考上面的"不使用 Docker"方案
```

---

## 📝 快速启动脚本

您可以创建一个启动脚本来简化流程：

**创建 `start.sh`：**
```bash
#!/bin/bash
# 启动 MindGraph 服务

echo "检查 Redis..."
if ! redis-cli ping > /dev/null 2>&1; then
    echo "启动 Redis..."
    sudo service redis-server start
fi

echo "检查 Qdrant..."
if ! curl -s http://localhost:6333/collections > /dev/null 2>&1; then
    echo "启动 Qdrant..."
    qdrant &
    sleep 2
fi

echo "启动 MindGraph 服务器..."
python main.py
```

**使用方式：**
```bash
chmod +x start.sh
./start.sh
```

---

## 📚 更多信息

- 详细文档：查看 `README.md`
- API 参考：`docs/API_REFERENCE.md`
- WSL 设置：`docs/WSL_SETUP.md`
- Redis 设置：`docs/REDIS_SETUP.md`
