# MindGraph 运行指南

## 📋 前置要求检查

根据检查结果：
- ✅ **Python 3.11.7** - 已安装（要求 3.8+）
- ✅ **Node.js v18.15.0** - 已安装（要求 18+）
- ❌ **Redis** - 未运行（必需）
- ❌ **Qdrant** - 未运行（知识空间功能必需）

## 🚀 运行步骤

### 第一步：启动 Redis 服务

**Windows 方式：**

1. **使用 WSL（推荐）：**
   ```bash
   # 在 WSL 中安装 Redis
   sudo apt update
   sudo apt install redis-server
   sudo service redis-server start
   
   # 验证
   redis-cli ping  # 应该返回: PONG
   ```

2. **使用 Docker：**
   ```bash
   docker run -d -p 6379:6379 redis:7-alpine
   ```

3. **Windows 原生安装：**
   - 下载：https://github.com/microsoftarchive/redis/releases
   - 解压后运行 `redis-server.exe`

### 第二步：启动 Qdrant 服务（知识空间功能需要）

**使用项目提供的脚本（推荐）：**
```bash
bash scripts/install_qdrant.sh
```

**或使用 Docker：**
```bash
docker run -d -p 6333:6333 qdrant/qdrant
```

**验证 Qdrant：**
```bash
curl http://localhost:6333/collections  # 应该返回 JSON
```

### 第三步：安装后端依赖

```bash
# 在项目根目录执行
python scripts/setup.py
```

这个脚本会：
- 安装所有 Python 依赖包
- 安装 Playwright 浏览器
- 验证安装

### 第四步：构建前端

```bash
cd frontend
npm install
npm run build
cd ..
```

**开发模式（可选）：**
```bash
cd frontend
npm run dev  # 前端开发服务器（热重载）
```

### 第五步：配置环境变量

检查 `.env` 文件是否已配置：

**必需配置：**
```bash
# LLM API Key（必需）
QWEN_API_KEY=your-qwen-api-key-here

# Redis 配置（必需）
REDIS_URL=redis://localhost:6379/0

# Qdrant 配置（知识空间功能必需）
QDRANT_HOST=localhost:6333

# 服务器配置
PORT=9527
HOST=0.0.0.0
DEBUG=False  # 开发时设置为 True
```

### 第六步：运行服务器

```bash
python main.py
```

## 🌐 访问入口

启动成功后，可以通过以下地址访问：

| 服务 | URL | 说明 |
|------|-----|------|
| **交互式编辑器** | http://localhost:9527/editor | 完整功能的 Web 编辑器 |
| **API 文档** | http://localhost:9527/docs | Swagger UI（仅 DEBUG 模式） |
| **管理面板** | http://localhost:9527/admin | API 密钥、用户、设置管理 |
| **健康检查** | http://localhost:9527/health | 服务器状态 |
| **知识空间** | http://localhost:9527/knowledge-space | 知识空间管理界面 |

## ⚠️ 重要提示

1. **Redis 必须在启动服务器前运行**，否则应用无法启动
2. **Qdrant 是知识空间功能必需的**，如果不需要该功能可以暂时不启动
3. **Celery 工作进程会自动启动**用于后台处理
4. **API 文档** (`/docs`) 仅在 `DEBUG=True` 时可用

## 🔧 故障排查

### Redis 连接失败
```bash
# 检查 Redis 是否运行
redis-cli ping

# Windows WSL 中启动
wsl sudo service redis-server start
```

### Qdrant 连接失败
```bash
# 检查 Qdrant 是否运行
curl http://localhost:6333/collections

# 使用 Docker 启动
docker run -d -p 6333:6333 qdrant/qdrant
```

### 端口被占用
```bash
# Windows 检查端口占用
netstat -ano | findstr :9527

# 修改 .env 中的 PORT 配置
PORT=9528  # 使用其他端口
```

### 前端构建失败
```bash
# 清除缓存重新安装
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run build
```

## 📚 更多信息

- 详细文档：查看 `README.md`
- API 参考：`docs/API_REFERENCE.md`
- WSL 设置：`docs/WSL_SETUP.md`
- Redis 设置：`docs/REDIS_SETUP.md`
