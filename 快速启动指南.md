# MindGraph 快速启动指南

## 📋 前置检查清单

在开始之前，确保以下服务已安装并运行：

- ✅ Python 3.8+（推荐 3.10+）
- ✅ Node.js 18+（推荐 20+）
- ✅ Redis（必需）
- ✅ Qdrant（知识空间功能需要）
- ✅ PostgreSQL（应用会自动启动）

---

## 🚀 完整启动步骤

### 第一步：打开终端并进入项目目录

**WSL/Linux：**
```bash
cd /mnt/d/MindMate/MindGraph-main
```

**Windows PowerShell：**
```powershell
cd d:\MindMate\MindGraph-main
```

---

### 第二步：检查并启动 Redis

**检查 Redis 是否运行：**
```bash
redis-cli ping
# 应该返回: PONG
```

**如果未运行，启动 Redis：**

**WSL/Linux：**
```bash
sudo service redis-server start
# 或
sudo systemctl start redis-server
```

**Windows（如果使用 WSL）：**
```powershell
wsl sudo service redis-server start
```

**验证：**
```bash
redis-cli ping  # 应该返回 PONG
```

---

### 第三步：检查并启动 Qdrant

**检查 Qdrant 是否运行：**
```bash
curl http://localhost:6333/collections
# 应该返回 JSON 数据
```

**如果未运行，启动 Qdrant：**

**方法 1：使用 systemd（如果已安装为服务）**
```bash
sudo systemctl start qdrant
```

**方法 2：直接运行（如果已安装）**
```bash
qdrant &
```

**方法 3：使用 Docker**
```bash
docker run -d -p 6333:6333 --name qdrant qdrant/qdrant
```

**验证：**
```bash
curl http://localhost:6333/collections
```

---

### 第四步：检查前端是否已构建

**检查构建目录：**
```bash
ls frontend/dist/index.html
# 如果文件存在，说明已构建
```

**如果未构建，构建前端：**

```bash
cd frontend

# 安装依赖（如果未安装）
npm install

# 构建前端
npm run build

# 返回项目根目录
cd ..
```

**验证构建：**
```bash
ls frontend/dist/index.html  # 应该存在
```

---

### 第五步：检查环境变量配置

**检查 .env 文件：**
```bash
# 确保 .env 文件存在
ls .env

# 检查关键配置（可选）
grep -E "QWEN_API_KEY|REDIS_URL|QDRANT_HOST|PORT" .env
```

**必需配置项：**
- `QWEN_API_KEY` - 您的通义千问 API 密钥
- `REDIS_URL=redis://localhost:6379/0`
- `QDRANT_HOST=localhost:6333`（知识空间功能需要）
- `PORT=9527`

---

### 第六步：运行服务器

**方法 1：直接运行（推荐）**
```bash
python main.py
```

**方法 2：使用启动脚本**
```bash
# WSL/Linux
chmod +x start.sh
./start.sh

# Windows PowerShell
.\start.ps1
```

---

### 第七步：验证服务器运行

服务器启动成功后，您会看到：

```
✓ Redis is ready
✓ Qdrant is ready
✓ Celery worker started
Server ready at: http://localhost:9527
```

**访问地址：**
- 编辑器：http://localhost:9527/editor
- 管理面板：http://localhost:9527/admin
- 健康检查：http://localhost:9527/health

---

## 🔧 一键启动（使用脚本）

如果您已经配置好所有服务，可以使用启动脚本自动检查并启动：

```bash
# WSL/Linux
chmod +x start.sh
./start.sh

# Windows PowerShell
.\start.ps1
```

启动脚本会自动：
1. ✅ 检查 Python 和 Node.js
2. ✅ 检查并启动 Redis
3. ✅ 检查并启动 Qdrant
4. ✅ 检查并构建前端
5. ✅ 检查环境变量
6. ✅ 启动服务器

---

## ⚠️ 常见问题

### Redis 未运行
```bash
# 错误：Connection refused
# 解决：
sudo service redis-server start
redis-cli ping  # 验证
```

### Qdrant 未运行
```bash
# 错误：Connection refused
# 解决：
qdrant &  # 或使用 systemd/docker
curl http://localhost:6333/collections  # 验证
```

### 前端未构建
```bash
# 警告：Vue SPA not available
# 解决：
cd frontend
npm install
npm run build
cd ..
```

### 端口被占用
```bash
# 错误：Address already in use
# 解决：
lsof -i :9527  # 查找占用进程
kill <PID>     # 终止进程
# 或修改 .env 中的 PORT
```

---

## 🛑 停止服务器

**方法 1：在运行服务器的终端按 `Ctrl + C`**

**方法 2：使用停止脚本**
```bash
./stop.sh  # WSL/Linux
.\stop.ps1  # Windows PowerShell
```

---

## 📝 快速参考命令

```bash
# 1. 进入项目目录
cd /mnt/d/MindMate/MindGraph-main

# 2. 启动 Redis
sudo service redis-server start

# 3. 启动 Qdrant
qdrant &

# 4. 构建前端（如果需要）
cd frontend && npm install && npm run build && cd ..

# 5. 运行服务器
python main.py

# 停止：按 Ctrl + C
```

---

## 💡 提示

1. **首次运行**：确保所有依赖已安装（`python scripts/setup/setup.py`）
2. **开发模式**：前端可以使用 `npm run dev` 单独运行（热重载）
3. **生产模式**：确保前端已构建（`npm run build`）
4. **日志查看**：日志文件在 `logs/app.log`

---

## 🎯 最简单的启动方式

如果所有服务都已配置好：

```bash
cd /mnt/d/MindMate/MindGraph-main
./start.sh
```

就这么简单！脚本会自动处理一切。
