# MindGraph 启动步骤（简化版）

## 🎯 最简启动流程（3 步）

### 步骤 1：打开终端并进入项目

```bash
cd /mnt/d/MindMate/MindGraph-main
```

### 步骤 2：启动必要服务

```bash
# 启动 Redis
sudo service redis-server start

# 启动 Qdrant（后台运行）
qdrant &
```

### 步骤 3：运行服务器

```bash
python main.py
```

**完成！** 服务器将在 http://localhost:9527 运行

---

## 🔍 如果遇到问题

### 问题 1：Redis 未安装
```bash
sudo apt update
sudo apt install redis-server -y
sudo service redis-server start
```

### 问题 2：Qdrant 未安装
```bash
bash scripts/setup/install_dependencies.sh --qdrant-only
qdrant &
```

### 问题 3：前端未构建
```bash
cd frontend
npm install
npm run build
cd ..
```

### 问题 4：Python 依赖未安装
```bash
python scripts/setup/setup.py
# 或
pip install -r requirements.txt
```

---

## 🛑 停止服务器

在运行服务器的终端窗口按：**`Ctrl + C`**

---

## 📱 访问地址

启动成功后访问：
- **编辑器**：http://localhost:9527/editor
- **管理面板**：http://localhost:9527/admin
