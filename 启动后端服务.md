# 启动后端服务指南

## 问题诊断

前端运行正常（端口 5173），但无法连接到后端服务（端口 9527）。

错误信息：
```
Error: connect ECONNREFUSED 127.0.0.1:9527
```

这表明后端服务未运行。

---

## 快速启动步骤

### 方法一：使用启动脚本（推荐）

在 **WSL 终端**中执行：

```bash
cd /mnt/d/MindMate/MindGraph-main
chmod +x start.sh
./start.sh
```

### 方法二：直接使用 Python 启动

在 **WSL 终端**中执行：

```bash
cd /mnt/d/MindMate/MindGraph-main
python main.py
```

或者：

```bash
cd /mnt/d/MindMate/MindGraph-main
python -m services.infrastructure.process.server_launcher
```

---

## 启动前检查

### 1. 检查后端服务状态

```bash
cd /mnt/d/MindMate/MindGraph-main
chmod +x check_backend.sh
./check_backend.sh
```

### 2. 如果端口被占用，先清理

```bash
cd /mnt/d/MindMate/MindGraph-main
chmod +x kill_port_9527.sh
./kill_port_9527.sh
```

---

## 启动后验证

启动成功后，你应该看到：

```
Environment: development (DEBUG=True)
Host: 0.0.0.0
Port: 9527
...
Server ready at: http://localhost:9527
```

然后访问以下地址验证：

- **健康检查**: http://localhost:9527/health
- **API 文档**: http://localhost:9527/docs
- **前端代理**: http://localhost:5173 (前端会自动代理到后端)

---

## 常见问题

### 1. 端口 9527 被占用

**解决方案：**
```bash
# 查找占用端口的进程
lsof -ti :9527

# 终止进程
lsof -ti :9527 | xargs kill -9

# 或使用清理脚本
./kill_port_9527.sh
```

### 2. PostgreSQL 端口 5432 被占用

**解决方案：**
```bash
# 查找占用端口的进程
lsof -ti :5432

# 终止进程
lsof -ti :5432 | xargs kill -9

# 或修改 PostgreSQL 端口（在 .env 文件中）
POSTGRESQL_PORT=5433
DATABASE_URL=postgresql://mindgraph_user:mindgraph_password@localhost:5433/mindgraph
```

### 3. Redis 未运行

**解决方案：**
```bash
# 启动 Redis
sudo service redis-server start

# 验证
redis-cli ping  # 应该返回 PONG
```

### 4. 依赖服务未启动

启动脚本会自动检查并启动以下服务：
- Redis
- PostgreSQL
- Qdrant
- Celery

如果自动启动失败，请手动启动它们。

---

## 完整启动流程

1. **清理旧进程**（如果有）
   ```bash
   ./kill_port_9527.sh
   ```

2. **检查服务状态**
   ```bash
   ./check_backend.sh
   ```

3. **启动后端服务**
   ```bash
   ./start.sh
   # 或
   python main.py
   ```

4. **验证服务运行**
   - 访问 http://localhost:9527/health
   - 检查前端是否能正常连接（刷新浏览器）

---

## 注意事项

1. **保持终端窗口打开**：后端服务需要在终端中持续运行
2. **不要关闭终端**：关闭终端会停止后端服务
3. **使用 Ctrl+C 停止服务**：正常停止服务，不要直接关闭终端
4. **检查日志**：如果启动失败，查看终端输出的错误信息

---

## 下一步

启动后端服务后：
1. 刷新前端页面（http://localhost:5173）
2. 前端应该能正常连接到后端 API
3. 如果仍有问题，检查浏览器控制台的错误信息
